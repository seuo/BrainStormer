import React, {Component} from 'react',
import {navigate} from '@reach/router';

class AddIdea extends Component {

    handleSubmit = (e) => {
        e.preventDeafult()

        var ideaData = new FormData(this.form);
        var data = {
            name:
        }
    }



}